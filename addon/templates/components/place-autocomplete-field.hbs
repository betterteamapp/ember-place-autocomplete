{{#if hasBlock}}
  {{yield this}}
{{else}}
  {{input
      value=value
      classBinding="inputClass"
      tabindex=tabindex
      disabled=disabled
      focus-out='focusOut'
      placeholder=placeholder
      bubbles=true
      autocomplete="new-password"
      type="search"
  }}
{{/if}}